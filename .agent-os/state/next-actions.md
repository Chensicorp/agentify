# Next actions

Studio is in **pre-activation** state. The only valid next action is:

1. **Run `activate-studio`** — scan the repo, build runtime knowledge, initialise roles, logs, and state.

---

*After activation, this file will list the top recommended tasks based on the repo scan.*

*Update this file at the end of every session.*
